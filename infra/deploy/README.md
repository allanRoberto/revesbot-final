# Deploy Base (Etapa 1)

Estrutura inicial para evolução de deploy por ambiente.

Sugestão de próximos artefatos:
- `staging.env`
- `production.env`
- `deploy_staging.sh`
- `deploy_production.sh`
- integração com PM2 (`infra/pm2/ecosystem.config.js`)
- integração com Nginx (`infra/nginx/revesbot.conf.example`)

Nesta etapa, os arquivos foram criados como base sem alterar pipeline atual.
